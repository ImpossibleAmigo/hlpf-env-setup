## Student
Name: Чеберяко Олексій Іванович
Group: 232/1

## Практичне заняття №4: DTO + class-validator + Pipes

### Що було зроблено:
- Реалізовано DTO для Categories та Products.
- Налаштовано глобальний ValidationPipe (whitelist, transform).
- Створено TrimPipe для автоматичної обрізки пробілів у рядках.
- Виправлено помилки міграцій TypeORM та структури БД.

### Результати:
- Валідація некоректних вхідних даних працює (код 400).
- TrimPipe успішно видаляє пробіли перед збереженням.
- Дані успішно зберігаються в PostgreSQL через TypeORM (код 201).
